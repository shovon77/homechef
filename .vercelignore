homechef/
design/
docs/
.backups/
*.sh
# Do not ignore scripts/ — postinstall runs scripts/ensure-nested-tslib.js (pnpm install on Vercel).
# Ignore heavy SQL-only assets under scripts if desired (optional size trim):
scripts/**/*.sql

